import { Domain } from "../src/tipe-data-intersection-interface";
describe("should must Intersection Interface", () => {
    it ("should must Intersection", () => {

        const domain: Domain = {
            id: "1",
            name: "Farid",
        };

        console.info(domain);

    })

})