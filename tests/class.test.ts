describe('class', () => {
    it('should must class', () => {
        class Customer {
        }

        class Order {
        }

        const customer: Customer = new Customer();
        const order: Order = new Order();
    })
})